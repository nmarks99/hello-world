use std::env;
mod helpers;

fn main() {

    is_repo(); 
    // let command = String::from("git rev-parse --is-inside-work-tree");
    // let out = helpers::sys_command(command);
    // let a = &String::from_utf8(out.stdout).unwrap()[..];

    // match a {
    //     "true\n" => println!("BRUH"),
    //     _ => println!("Output from cmd is: {} ",a),
    // }

}

